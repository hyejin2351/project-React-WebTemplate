//
// Schema
//
const d = require('debug')('app:mod-sample');

//
// See https://www.apollographql.com/docs/apollo-server/example.html
//
const types = `
`;

const queries = `
`;

const mutations = `
`;

const resolvers = {
  Query: {
  },

  Mutation: {
  }
};

module.exports = {
  types,
  queries,
  mutations,
  resolvers,
};

